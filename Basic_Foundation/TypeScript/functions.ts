function add(numOne: number, numTwo: number): number {
    const numThree: number = numOne + numTwo;
    return numThree;
}

const num1: number = 1;
const num2: number = 2;

const result: number = add(num1, num2);
console.log(result);
